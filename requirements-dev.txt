pre-commit>=3.5.0
pytest>=7.0.0
ruff>=0.9.0
# Used by dialogue_manager tests and ROS2 action client unit tests (run_tests.sh).
numpy>=1.24
# Required by ROS 2 launch imports used in launch profile unit tests.
lark>=1.1
