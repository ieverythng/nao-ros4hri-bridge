# Previous package boundary

The earlier bundle is `docs/artifacts/nao_ros4hri_model_agnostic_evidence_2026-07-20.zip`. Its listing was checked on 22 July 2026 with a search for the F13/F14 run identifiers and the failure profiles `fail_once_pick`, `delivery_blocked`, `recipient_missing`, `every_other`, and `random_seeded`.

The check returned no matching entries. The current addendum therefore adds the F13 semantic-audit and F14 targeted-hardening evidence rather than duplicating files already present in the earlier ZIP. The prior bundle remains the authority for the v34 frozen primary score, model-agnostic comparison, and earlier Qwen3VL ablations.
