# Supervisor Change-Request Ledger

This ledger maps the supervisor comments extracted from `prism-uploads/TFM_reformatted_preview_RRE.pdf`, `prism-uploads/TFM_JUANv0_RRE.pdf`, and the handwritten screenshot notes in `prism-uploads/image.png` to the current thesis sources. It records whether each request is already implemented, newly reinforced in the Chapter 6--7 pass, or outside the latest focused edit scope.

| Request / comment theme | Source of request | Current implementation | Status |
| --- | --- | --- | --- |
| Ground the thesis in the complete NAO implementation, not only `planner_llm`. | Screenshot; reformatted PDF p. 11 comments; v0 PDF p. 8 comments. | Title names an interaction and execution stack rather than an LLM planner (`main.tex`). Introduction states that the claim spans dialogue lifecycle, routing, grounding, planning, orchestration, robot skills, and feedback (`sections/01_introduction.tex`). Chapter 6 now opens with the complete NAO stack and lists the scored subsystems (`sections/06_validation_methodology.tex`). Chapter 7 now treats evidence as integrated-stack calibration/final-run evidence rather than planner-only performance (`sections/07_results.tex`). | Implemented and reinforced. |
| Remove thesis-facing emphasis on ROS4HRI and avoid presenting ROS4HRI as the thesis object. | Screenshot; reformatted PDF pp. 11, 13; v0 PDF pp. 1, 9. | Title no longer includes ROS4HRI (`main.tex`). Background keeps ROS4HRI only as reusable HRI representation/interface context (`sections/02_background.tex`). Architecture and validation wording use ROS 2/NAO stack language and make ROS a transport/interface framework rather than the research contribution. | Implemented. |
| Put research questions before objectives. | Screenshot; reformatted PDF p. 11. | Introduction order is Problem Statement, Research Questions, then Objectives (`sections/01_introduction.tex`). | Implemented. |
| Add thesis/document structure explaining what each chapter contains. | Screenshot; reformatted PDF p. 17. | `Document Structure` section enumerates the role of Chapters 2--10 (`sections/01_introduction.tex`). | Implemented. |
| Split ROS and HRI concepts in the literature review; expand HRI, abstraction cascade, symbolic grounding, and modularity. | Screenshot; reformatted PDF p. 13. | Background separates `Human-Robot Interaction`, `ROS 2 for Distributed Robot Software`, and `Reusable HRI Representations and Interfaces` (`sections/02_background.tex`). The introduction and background now emphasize modular combinations of language models, symbolic grounding, execution validation, and robot capabilities. | Implemented. |
| Use generic terms in background; avoid describing the implemented approach as related work. | Reformatted PDF pp. 14--16. | Background presents general concepts and references; implementation-specific node/package details are moved to architecture, contracts, and implementation chapters (`sections/03_architecture_requirements.tex`, `sections/04_runtime_contracts.tex`, `sections/05_implementation.tex`). | Implemented. |
| Explain that the contribution is modular architecture for reasoning, planning, acting, and recovery over symbolic world state, intentions, and robot capabilities. | Screenshot; reformatted PDF p. 13. | Introduction problem statement and RQs frame the work as language-guided dialogue, symbolic grounding, task planning, deterministic execution, recovery, and evidence-based communication (`sections/01_introduction.tex`). Chapter 6 validation goals now explicitly test routing, grounding, target preservation, execution supervision, failure handling, speech ownership, traceability, and state freshness (`sections/06_validation_methodology.tex`). | Implemented and reinforced. |
| Make the orchestrator monitor plan execution. | Reformatted PDF p. 18. | Architecture and implementation describe `nao_orchestrator` as validating plans, coordinating skill invocation, monitoring execution progress, and emitting structured feedback (`sections/03_architecture_requirements.tex`, `sections/05_implementation.tex`). Validation requires Contract 6 results and Contract 7 feedback with lineage (`sections/06_validation_methodology.tex`). | Implemented. |
| Ensure all tables and figures are introduced/referenced in text. | Screenshot; reformatted PDF p. 18. | Chapters 6 and 7 introduce each table before it appears, including validation levels, execution profiles, validation goals, questionnaire, fake-skill manifest, robustness set, final-run ledger, run evidence, semantic findings, fake-policy results, ablation, interaction contracts, and RQ synthesis. Existing architecture/implementation tables and figures are also introduced in their surrounding text. | Implemented for Chapters 6--7; broadly checked. |
| Properly introduce sections before lists or tables. | Screenshot; reformatted PDF p. 19. | Chapter 6 sections now start with explanatory prose before tables/longtables. Chapter 7 now opens with an evidence-set framing section and adds prose before all result tables. | Implemented and reinforced. |
| Use parenthetical citation style where appropriate. | Screenshot; reformatted PDF p. 13. | Main source uses `biblatex` authoryear style. General background statements primarily use `\parencite{...}` and narrative references use `\textcite{...}` only where the author is grammatically part of the sentence. | Implemented; final bibliography polish may still review individual prose preferences. |
| Add NAO robot image and past NAO references. | Reformatted PDF p. 14. | The title page includes the UAB logo, not a robot image. NAO is defined as the platform and implementation target. This item is global and outside the Chapter 6--7 focused pass; it should be checked in the background/implementation figures if the supervisor still expects a dedicated robot image. | Partially implemented / optional follow-up. |
| Avoid saying NAO “navigates” too strongly. | Reformatted PDF p. 14. | Current text mostly refers to navigation adapters, route cases, or movement within evaluated conditions. Validation distinguishes software trajectory/fake-skill evidence from physical navigation accuracy (`sections/06_validation_methodology.tex`). | Implemented in validation wording; remaining prose can be globally searched again if desired. |
| Prepare Sections 6 and 7 for the final frozen run rather than overclaiming July 13/14 diagnostics. | User request; July evidence concerns in current draft. | Chapter 6 now defines final-run gates and evidence-bundle requirements. Chapter 7 separates pre-final calibration evidence from the final frozen run, adds `Final frozen run reporting ledger`, and states that July 13/14 artifacts do not define the final integrated-stack score (`sections/07_results.tex`). | Implemented in latest pass. |
| Preserve evidence that the stack can handle known KB mutation, interactions, and real/fake skill dispatch without overstating incomplete reporting semantics. | User request. | Chapter 7 keeps F28/F08/F10/F14 as calibration evidence for public ingress, KB mutation, fake-skill policies, known interactions, and dispatch, while explicitly naming remaining reporting/state/selection gates for the final run (`sections/07_results.tex`). | Implemented in latest pass. |
| Keep detector/perception evidence separate from semantic-stack score. | User request; supervisor focus on whole-system claim boundaries. | Chapter 6 validation levels and execution profiles separate detector-enabled and embodied claims. Chapter 7 repeats that detector object recognition is not merged with KB-authored semantic-stack evidence. | Implemented. |
| Create a traceable final-run checklist. | User request. | Chapter 7 includes a manifest-backed final frozen run ledger with required rows for E2E-main, deterministic fake policies, robustness variants, state/fixture checks, speech/interface checks, and source/artifact fingerprint (`sections/07_results.tex`). | Implemented in latest pass. |