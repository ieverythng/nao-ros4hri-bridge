\chapter{System Design}
\label{chap:architecture-requirements}
% ============================================================

\section{Functional Requirements}

Table~\ref{tab:functional-requirements} lists the functional requirements for the complete interaction and execution stack. They cover natural-language ingress, dialogue routing, symbolic grounding, planning, deterministic execution, feedback, and observability. Their implementation owners are introduced in Section~\ref{sec:high-level-ros-architecture} and detailed in Chapter~\ref{chap:implementation}.

\begin{table}[H] \centering \small \begin{tabular}{p{0.14\linewidth}p{0.78\linewidth}} \toprule \textbf{ID} & \textbf{Requirement}\\ \midrule FR1 & The system shall accept natural-language user input through the dialogue stack.\\
FR2 & The system shall distinguish dialogue-only turns from execution-eligible turns.\\
FR3 & The system shall convert execution-eligible user turns into admitted planner requests before invoking the planner.\\
FR4 & The planner shall generate structured skill plans over an explicit skill registry.\\
FR5 & The orchestrator shall validate planner outputs before any robot skill is dispatched.\\
FR6 & The system shall publish execution feedback after relevant plan and step/skill events.\\
FR7 & The planner shall support asking for clarification, replanning and plan failure when execution feedback indicates that the current plan cannot continue safely.\\
FR8 & The system shall expose traces, logs, or runtime messages suitable for validation of the stack.\\
\bottomrule \end{tabular} \caption{Functional requirements for the proposed interaction and execution architecture.} \label{tab:functional-requirements} \end{table}

\section{Design Principles}

Four responsibility principles guide the implementation of Table~\ref{tab:functional-requirements}. They define the component roles before the corresponding ROS~2 package names are introduced.

\begin{enumerate}
  \item \textbf{Dialogue remains distinct from execution.} Greetings, conversation, and questions about the robot's known state remain non-executable unless the user explicitly requests an action.
  \item \textbf{The planner proposes structured skill plans.} It selects from the reviewed registry and uses grounded scene context, but does not execute skills, speak directly, or call robot-specific APIs.
  \item \textbf{The orchestrator remains the execution gate.} It admits execution requests, validates plans, dispatches skills, and publishes typed execution feedback.
  \item \textbf{Skills own fresh execution evidence.} Perception and execution skills query the knowledge base (KB) or robot state when required. Planning-time context cannot establish that a later action succeeded.
\end{enumerate}

% ============================================================
\section{High-Level Architecture}
\label{sec:high-level-ros-architecture}
% ============================================================

The design separates dialogue, grounding, planning, orchestration, and robot skills into components with distinct responsibilities. Figure~\ref{fig:layered-architecture} presents these components before their individual roles are described.

% ============================================================
% High-level layered architecture
% ============================================================

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  font=\footnotesize,
  >=Latex,
  component/.style={
    draw,
    rounded corners,
    align=center,
    text width=2.45cm,
    minimum height=1.05cm,
    inner sep=4pt
  },
  layer/.style={draw,rounded corners,dashed,inner sep=7pt},
  control/.style={-{Latex[length=2.2mm]}, line width=0.6pt},
  support/.style={-{Latex[length=2.0mm]}, line width=0.55pt, dashed},
  edge/.style={font=\scriptsize,fill=white,inner sep=1pt}
]
\node[component,fill=blue!7] (dialogue) {Dialogue manager};
\node[component,fill=blue!7,right=0.35cm of dialogue] (language) {chatbot};
\node[layer,fit=(dialogue)(language),label={[font=\small]above:\textbf{Dialogue}}] (dialoguelayer) {};

\node[component,fill=green!8,right=0.85cm of language] (planner) {Planner};
\node[layer,fit=(planner),label={[font=\small]above:\textbf{Planning}}] (planninglayer) {};

\node[component,fill=purple!7,right=0.85cm of planner] (orchestrator) {Orchestrator};
\node[component,fill=purple!7,right=0.35cm of orchestrator] (skills) {Robot skills};
\node[layer,fit=(orchestrator)(skills),label={[font=\small]above:\textbf{Execution}}] (executionlayer) {};

\node[component,fill=orange!10,below=1.25cm of planner,text width=5.2cm] (grounding) {Grounding and knowledge components\\current people, objects, locations, and relations};
\node[layer,fit=(grounding),label={[font=\small]below:\textbf{Grounding}}] (groundinglayer) {};

\draw[control] (dialogue) -- (language);
\draw[control] (language) -- node[edge,above]{execution goal} (planner);
\draw[control] (planner) -- node[edge,above]{plan} (orchestrator);
\draw[control] (orchestrator) -- node[edge,above]{validated step} (skills);
\draw[support] (grounding) -- (language);
\draw[support] (grounding) -- (planner);
\draw[support] (skills.south) to[out=-90,in=0] node[edge,below]{fresh evidence} (grounding.east);
\draw[control] (orchestrator.north) to[out=90,in=90] node[edge,above]{execution feedback} (planner.north);
\end{tikzpicture}
\caption{High-level architecture. The language and grounding components provide the planner with the user's goal and current symbolic context. The planner proposes a skill sequence; the orchestrator validates and coordinates it; and the skills return fresh execution evidence.}
\label{fig:layered-architecture}
\end{figure}

The main components are:

\begin{itemize}
  \item The \textbf{dialogue manager} maintains the conversation and is responsible for user-facing speech.
  \item The \textbf{language component} interprets each request, distinguishes conversation or knowledge questions from action requests, and extracts an execution goal when needed.
  \item The \textbf{grounding and knowledge components} provide a compact description of relevant people, objects, locations, and relations without presenting raw sensor data to the language model.
  \item The \textbf{planner} converts an admitted execution goal into an ordered sequence of available robot skills. It proposes actions but does not execute them.
  \item The \textbf{orchestrator} coordinates robot behaviour. It validates the proposed plan, invokes its skills in order, monitors their results, and returns execution feedback.
  \item The \textbf{skills} implement perception, movement, manipulation, motion, and communication capabilities. Each skill returns current evidence about its outcome.
\end{itemize}

% ============================================================
% Runtime flow
% ============================================================

\section{Runtime Flow}

During a normal turn, the dialogue manager receives the user's request and passes it to the language component together with the relevant conversation state. The language component also receives a compact description of the current world. It then selects one of three outcomes: ordinary dialogue, a knowledge answer, or an execution request. Dialogue and knowledge questions return directly to the dialogue manager and do not activate the planner.

For an execution request, the extracted goal and grounded entities are passed to the planner. The planner selects from the available skills and proposes an ordered plan. The orchestrator checks that the plan contains supported skills and valid arguments before invoking anything on the robot.

The orchestrator then executes the accepted steps in order. Each skill returns a result based on current robot or world evidence. These results allow the orchestrator to complete the plan, stop truthfully, or return feedback for replanning or clarification. The dialogue manager communicates the final outcome to the user. Figure~\ref{fig:runtime-sequence} summarises this flow.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
  font=\footnotesize,
  >=Latex,
  node distance=0.38cm,
  box/.style={
    draw,
    rounded corners,
    align=center,
    text width=8.2cm,
    minimum height=0.66cm,
    fill=gray!5,
    inner sep=4pt
  },
  dialogue/.style={box,fill=blue!7},
  planning/.style={box,fill=green!8},
  execution/.style={box,fill=purple!7},
  arr/.style={-{Latex[length=2.4mm]},line width=0.65pt},
  side/.style={-{Latex[length=2.2mm]},line width=0.55pt,dashed},
  edgelabel/.style={font=\scriptsize,fill=white,inner sep=1.2pt}
]

\node[box] (u) {1. User request};

\node[dialogue,below=of u] (dm)
{2. Dialogue manager: maintain the turn and conversation};

\node[dialogue,below=of dm] (c)
{3. Language component: choose dialogue, knowledge, or execution and extract the goal};

\node[planning,below=of c] (p)
{4. Planner: produce an ordered sequence of available skills for execution requests};

\node[execution,below=of p] (o)
{5. Orchestrator: validate the plan and execute its steps in order};

\node[execution,below=of o] (s)
{6. Skills: act on the robot or environment and return fresh evidence};

\node[dialogue,below=of s] (d)
{7. Dialogue manager: communicate completion, clarification, or failure};

\draw[arr] (u) -- (dm);
\draw[arr] (dm) -- (c);
\draw[arr] (c) -- node[edgelabel,right]{execution requests only} (p);
\draw[arr] (p) -- (o);
\draw[arr] (o) -- (s);
\draw[arr] (s) -- node[edgelabel,right]{result} (d);
\draw[side] (s.west) to[out=180,in=180] node[edgelabel,left]{failure feedback or replan} (p.west);
\end{tikzpicture}
\caption{Runtime sequence from a user request to an evidence-based response. Dialogue and knowledge turns stop after interpretation; only execution requests continue through planning and skill execution.}
\label{fig:runtime-sequence}
\end{figure}

% ============================================================
